# Supply-chain hardening (port từ Pi: save-exact + min-release-age).
# package-lock.json là ground truth duy nhất của dependency.
save-exact=true
min-release-age=2
# Không bao giờ chạy lifecycle script khi install/ci (Pi dùng --ignore-scripts).
ignore-scripts=true
fund=false
audit=true
